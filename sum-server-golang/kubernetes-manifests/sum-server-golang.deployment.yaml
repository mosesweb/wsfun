# This Deployment manifest defines:
# - single-replica deployment of the container image, with label "app: sum-server-golang"
# - Pod exposes port 8080
# - specify PORT environment variable to the container process
# Syntax reference https://kubernetes.io/docs/concepts/configuration/overview/
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sum-server-golang
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sum-server-golang
  template:
    metadata:
      labels:
        app: sum-server-golang
    spec:
      containers:
      - name: server
        image: sum-server-golang
        ports:
        - containerPort: 8080
        env:
        - name: PORT
          value: "8080"
        readinessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 5
